import FilterSidebar from "../FilterSidebar";

export default function FilterSidebarExample() {
  return (
    <div className="max-w-xs p-4 bg-card rounded-md border">
      <FilterSidebar />
    </div>
  );
}
